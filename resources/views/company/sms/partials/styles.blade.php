{{-- Company SMS pages reuse the exact same editorial sx-* system as the owner
     side (built on the shared --bk-* theme tokens), so the two experiences feel
     like one product. --}}
@include('owner.sms.partials.styles')
