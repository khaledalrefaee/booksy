[phases.setup]
nixPkgs = ["liberation_ttf", "dejavu_fonts"]

[phases.build]
cmds = []

[start]
cmd = "php artisan config:clear && php artisan migrate --force && php artisan storage:link --force && php artisan serve --host=0.0.0.0 --port=$PORT"
